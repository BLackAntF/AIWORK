# 任务：番茄叶片病害档案功能实现

## 概述

新增番茄叶片病害档案功能，实现检测结果与AI问答联动。系统新增 `disease_profiles` 表存储10种番茄病害的标准化档案（诱因/症状/预防/治疗/药剂），检测时自动关联档案，AI问答时优先使用档案数据，保证回答一致性。

---

## 后端任务

### 1. 新增病害档案模型

**文件**：`backend/models/disease_profile.py`（新建）

```python
class DiseaseProfile(db.Model):
    __tablename__ = 'disease_profiles'

    id = db.Column(db.Integer, primary_key=True)
    disease_id = db.Column(db.String(50), unique=True, nullable=False)
    disease_name = db.Column(db.String(100), nullable=False)
    class_id = db.Column(db.Integer, nullable=False)  # YOLO分类ID 0-9

    causes = db.Column(db.Text)        # 诱因
    symptoms = db.Column(db.Text)       # 典型症状
    occurrence = db.Column(db.Text)    # 发生规律
    prevention = db.Column(db.Text)    # 预防措施
    treatment = db.Column(db.Text)     # 治疗方案
    pesticides = db.Column(db.Text)    # 推荐药剂

    is_active = db.Column(db.Boolean, default=True)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

**注册**：在 `backend/models/__init__.py` 添加导入

### 2. 初始化10种番茄病害数据

**文件**：`backend/models/disease_data.py`（新建）

包含10种病害数据（class_id: 0-9）：
- 0: BacterialSpot - 细菌性斑点病
- 1: EarlyBlight - 早疫病
- 2: Healthy - 健康叶片
- 3: LateBlight - 晚疫病
- 4: LeafMold - 叶霉病
- 5: MosaicVirus - 花叶病毒
- 6: SeptoriaLeafSpot - 斑枯病
- 7: SpiderMites - 蜘蛛螨
- 8: TargetSpot - 靶斑病
- 9: YellowLeafCurlVirus - 黄化曲叶病毒

**初始化脚本**：`backend/scripts/init_disease_profiles.py`

### 3. 新增 Service

**文件**：`backend/services/disease_profile_service.py`（新建）

```python
class DiseaseProfileService:
    def get_by_class_id(self, class_id):
        """根据YOLO分类ID获取病害档案"""

    def get_profile_for_llm(self, class_id):
        """获取适合给LLM的档案上下文"""
        profile = self.get_by_class_id(class_id)
        if not profile:
            return None
        return {
            "disease_name": profile.disease_name,
            "causes": profile.causes,
            "symptoms": profile.symptoms,
            "prevention": profile.prevention,
            "treatment": profile.treatment,
            "pesticides": profile.pesticides
        }
```

### 4. 修改检测接口

**文件**：`backend/routes/detection.py`

修改 `/api/detection/detect` 接口，在检测结果中附加病害档案：

```python
from services.disease_profile_service import disease_profile_service

# 检测结果中附加 disease_profile
for result in results:
    profile = disease_profile_service.get_profile_for_llm(result['class_id'])
    result['disease_profile'] = profile
```

返回数据结构：
```json
{
  "detection_results": [
    {
      "class_id": 1,
      "class_name": "EarlyBlight",
      "confidence": 0.95,
      "bbox": [x, y, w, h],
      "disease_profile": {
        "disease_name": "早疫病",
        "causes": "...",
        "symptoms": "...",
        "prevention": "...",
        "treatment": "...",
        "pesticides": "..."
      }
    }
  ]
}
```

### 5. 修改 AI 问答逻辑

**文件**：`backend/services/llm_service.py`

修改 `generate_answer` 方法，优先使用病害档案：

```python
def generate_answer(self, question, knowledge_list=None, detection_context='', disease_profile=None):
    context_parts = []

    # 1. 优先添加病害档案
    if disease_profile:
        profile_text = f"""【病害档案】
病害名称：{disease_profile['disease_name']}
诱因：{disease_profile['causes']}
症状：{disease_profile['symptoms']}
预防措施：{disease_profile['prevention']}
治疗方案：{disease_profile['treatment']}
推荐药剂：{disease_profile['pesticides']}"""
        context_parts.append(profile_text)

    # 2. 添加知识库内容
    if knowledge_list:
        context_parts.append(f"【相关知识】\n{chr(10).join(knowledge_list)}")

    # 3. 构建系统提示词
    system_prompt = f"""你是一个专业的番茄病害诊断助手。
请根据以下信息回答用户问题，回答要准确、专业、一致。
{chr(10).join(context_parts)}"""
    # ...
```

**文件**：`backend/routes/knowledge.py`

修改 `/api/knowledge/ask` 接口，接收 `detected_class_id` 参数：

```python
@knowledge_bp.route('/ask', methods=['POST'])
@login_required
def ask_question(current_user):
    data = request.get_json() or {}
    question = data.get('question', '').strip()
    detection_context = data.get('detection_context', '').strip()
    detected_class_id = data.get('detected_class_id')  # 新增

    # 获取病害档案
    disease_profile = None
    if detected_class_id is not None:
        disease_profile = disease_profile_service.get_profile_for_llm(detected_class_id)

    # 知识库检索
    knowledge_list = knowledge_service.search(question, top_k=3)

    # 生成回答
    llm_result = llm_service.generate_answer(
        question=question,
        knowledge_list=knowledge_list,
        detection_context=detection_context,
        disease_profile=disease_profile  # 新增
    )
```

---

## 前端任务

### 1. 检测结果页面展示病害档案

**文件**：`frontend/src/views/user/DetectionResult.vue`（或相关页面）

在检测结果区域下方新增病害档案卡片：

```vue
<template>
  <div v-if="result.disease_profile" class="disease-profile-card">
    <el-card>
      <template #header>
        <span>病害档案</span>
      </template>
      <el-descriptions :column="1" border>
        <el-descriptions-item label="病害名称">
          {{ result.disease_profile.disease_name }}
        </el-descriptions-item>
        <el-descriptions-item label="诱因">
          {{ result.disease_profile.causes }}
        </el-descriptions-item>
        <el-descriptions-item label="典型症状">
          {{ result.disease_profile.symptoms }}
        </el-descriptions-item>
        <el-descriptions-item label="预防措施">
          {{ result.disease_profile.prevention }}
        </el-descriptions-item>
        <el-descriptions-item label="治疗方案">
          {{ result.disease_profile.treatment }}
        </el-descriptions-item>
        <el-descriptions-item label="推荐药剂">
          {{ result.disease_profile.pesticides }}
        </el-descriptions-item>
      </el-descriptions>
    </el-card>
  </div>
</template>
```

### 2. AI 问答入口优化

**场景**：用户查看检测结果后，点击"咨询此病害"按钮

**修改**：在检测结果页面新增"咨询病害"按钮，点击后跳转/展开AI问答，并自动传递 `detected_class_id`：

```vue
<template>
  <el-button type="primary" @click="askAboutDisease(result.class_id)">
    咨询此病害
  </el-button>
</template>

<script setup>
const askAboutDisease = (classId) => {
  // 跳转或展开AI问答组件，传递 detected_class_id
  // 当前端有检测结果上下文时，询问时自动带上 detected_class_id
};
</script>
```

### 3. AI 问答组件传递参数

**文件**：`frontend/src/components/ChatAssistant.vue`（或相关组件）

修改问答接口调用，传递 `detected_class_id`：

```javascript
const sendMessage = async () => {
  await request.post('/api/knowledge/ask', {
    question: userInput.value,
    session_id: currentSessionId,
    detection_context: detectionContext,  // 当前检测结果
    detected_class_id: detectedClassId   // 当前识别到的病害ID
  });
};
```

---

## 数据示例

### EarlyBlight（早疫病）数据示例

```json
{
  "disease_id": "EarlyBlight",
  "disease_name": "早疫病",
  "class_id": 1,
  "causes": "由半知菌亚门链格孢属真菌引起。病原菌可在病残体或种子表面越冬，通过气流、雨水传播。高温高湿（25-30°C，相对湿度80%以上）利于发病。",
  "symptoms": "叶片初期出现暗褐色或黑色小斑点，逐渐扩大成圆形或不规则形病斑，直径3-12mm。病斑有同心轮纹，中央产生黑色霉层。严重时叶片枯黄脱落，影响果实产量和品质。",
  "occurrence": "结果期发病最重。一般春播番茄在6-8月发病严重。种植过密、通风不良、排水不畅田块发病重。",
  "prevention": "1. 选用抗病品种；2. 实行轮作（与非茄科作物轮作2-3年）；3. 合理密植，加强通风；4. 科学施肥，增施磷钾肥；5. 及时清除病残体，带出田外销毁。",
  "treatment": "发病初期及时喷药保护。每隔7-10天喷一次，连续2-3次。常用药剂及浓度：75%百菌清可湿性粉剂600倍液、25%嘧菌酯悬浮剂1500倍液、43%戊唑醇悬浮剂3000倍液、10%苯醚甲环唑水分散粒剂2000倍液。注意药剂交替使用，避免产生抗药性。",
  "pesticides": "75%百菌清可湿性粉剂（600倍）、25%嘧菌酯悬浮剂（1500倍）、43%戊唑醇悬浮剂（3000倍）、10%苯醚甲环唑水分散粒剂（2000倍）、80%代森锰锌可湿性粉剂（800倍）"
}
```

---

## 注意事项

1. `class_id` 必须与YOLO模型分类ID一致（0-9）
2. 病害档案是结构化数据，LLM回答更一致
3. 知识库文章仍保留作为补充
4. 前端展示时注意处理 disease_profile 为空的情况（健康叶片）

---

## 预期效果

```
用户上传图片 → 检测识别（早疫病） → 显示结果+病害档案
                                        ↓
用户点击"咨询此病害" → AI问答（已携带档案） → 回答一致、专业
```
