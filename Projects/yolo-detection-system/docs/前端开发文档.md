# 前端开发文档

## 一、设计规范

### 1.1 配色方案

#### 亮色主题（陶土红）
```css
:root {
  /* 背景色 */
  --color-bg-primary: #FFFFFF;
  --color-bg-secondary: #F8F9FA;
  --color-bg-tertiary: #F1F3F5;

  /* 文字色 */
  --color-text-primary: #1A1A2E;
  --color-text-secondary: #4A4A68;
  --color-text-muted: #6C757D;
  --color-text-inverse: #FFFFFF;

  /* 点缀色 - 陶土红 */
  --color-accent: #E07A5F;
  --color-accent-hover: #C96A52;
  --color-accent-light: #F5E6E2;

  /* 功能色 */
  --color-success: #6B8E23;
  --color-warning: #D4A574;
  --color-error: #CD5C5C;
  --color-info: #5D7B93;

  /* 边框 */
  --color-border: #DEE2E6;
  --color-border-light: #E9ECEF;

  /* 阴影 */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.07);
  --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
}
```

#### 暗色主题（黑金）
```css
[data-theme="dark"] {
  /* 背景色 */
  --color-bg-primary: #0A0A0A;
  --color-bg-secondary: #141414;
  --color-bg-tertiary: #1E1E1E;

  /* 文字色 */
  --color-text-primary: #F5F5F5;
  --color-text-secondary: #B8B8B8;
  --color-text-muted: #808080;
  --color-text-inverse: #0A0A0A;

  /* 点缀色 - 金色 */
  --color-accent: #D4AF37;
  --color-accent-hover: #F4CF57;
  --color-accent-light: #2A2518;

  /* 功能色 */
  --color-success: #8FBC8F;
  --color-warning: #DAA520;
  --color-error: #CD5C5C;
  --color-info: #7EB6CF;

  /* 边框 */
  --color-border: #2D2D2D;
  --color-border-light: #1E1E1E;

  /* 阴影 */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.5);
}
```

### 1.2 字体规范
```css
:root {
  /* 主字体 - 中文 */
  --font-family-cn: "PingFang SC", "Microsoft YaHei", "Hiragino Sans GB", sans-serif;

  /* 特殊字体 - Logo/品牌标识 */
  --font-family-logo: 'Azonix', sans-serif;

  /* 数字/代码/英文 */
  --font-family-mono: "JetBrains Mono", "Fira Code", "SF Mono", "Consolas", monospace;

  /* 字体大小 */
  --font-size-xs: 12px;
  --font-size-sm: 14px;
  --font-size-base: 16px;
  --font-size-lg: 18px;
  --font-size-xl: 20px;
  --font-size-2xl: 24px;
  --font-size-3xl: 30px;

  /* 字体粗细 */
  --font-weight-light: 300;
  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;

  /* 行高 */
  --line-height-tight: 1.25;
  --line-height-normal: 1.5;
  --line-height-relaxed: 1.75;
}
```

### 1.3 视觉效果

#### 粒子连线网络背景
- **组件位置**：`App.vue` 最外层，所有页面共享同一个背景
- **层级保障**：背景 `zIndex: 0`，内容容器 `relative z-10`
- **配置参数**：
  - 粒子数量：80
  - 连线距离：150px
  - 鼠标光晕范围：80px
  - 粒子颜色：`var(--color-accent)`
  - 鼠标光晕颜色：`rgba(224, 122, 95, 0.2)`（亮色）/ `rgba(212, 175, 55, 0.2)`（暗色）
- **禁止**：不要在单个页面组件内单独使用背景

#### 发光效果类

**发光边框（`.glow-border`）**
- 悬停时边框发光 + 渐变描边
- 配合 `--color-accent` 自动应用主题色

**发光按钮（`.btn-glow`）**
- 鼠标跟随径向光晕
- 悬停上浮 + 外发光
- 配合 `--color-accent` 自动应用主题色

#### 毛玻璃效果类

| 类名 | 用途 | 适用场景 |
|------|------|---------|
| `.glass` | 通用毛玻璃 | 一般容器 |
| `.glass-card` | 卡片毛玻璃 | 内容卡片 |
| `.glass-navbar` | 导航栏毛玻璃 | 顶部导航 |
| `.glass-modal` | 弹窗毛玻璃 | 模态框 |

### 1.4 Element Plus 主题定制

Element Plus 主色调与设计规范对齐：

```css
/* 亮色主题 */
:root {
  --el-color-primary: #E07A5F;
  --el-color-primary-light-3: #E99A85;
  --el-color-primary-light-5: #F0B8A8;
  --el-color-primary-light-7: #F5D4CA;
  --el-color-primary-light-8: #F9E3DC;
  --el-color-primary-light-9: #FCF0ED;
  --el-color-primary-dark-2: #B8644E;
  --el-color-success: #6B8E23;
  --el-color-warning: #D4A574;
  --el-color-danger: #CD5C5C;
  --el-color-info: #5D7B93;
}

/* 暗色主题 */
[data-theme="dark"] {
  --el-color-primary: #D4AF37;
  --el-color-primary-light-3: #DCC05F;
  --el-color-primary-light-5: #E8D17F;
  --el-color-primary-light-7: #F0E0A0;
  --el-color-primary-light-8: #F5E8BC;
  --el-color-primary-light-9: #FAF0DC;
  --el-color-primary-dark-2: #B08D2D;
  --el-color-success: #8FBC8F;
  --el-color-warning: #DAA520;
  --el-color-danger: #CD5C5C;
  --el-color-info: #7EB6CF;
}
```

---

## 二、技术栈

| 组件 | 选型 | 版本 |
|------|------|------|
| 框架 | Vue 3 | 3.x (Composition API) |
| 构建工具 | Vite | 5.x |
| UI 框架 | Element Plus | 2.x |
| 路由 | Vue Router | 4.x |
| 状态管理 | Pinia | 2.x |
| HTTP 客户端 | Axios | 1.x |
| 图表库（可选） | ECharts | 5.x |
| 语言 | JavaScript / TypeScript | - |

---

## 二、项目结构

```
frontend/
├── public/
│   └── favicon.ico
│
├── src/
│   ├── api/                    # API 接口
│   │   ├── axios.js            # axios 封装
│   │   ├── auth.js             # 认证接口
│   │   ├── detection.js        # 检测接口
│   │   ├── knowledge.js        # 知识库问答接口
│   │   └── history.js          # 历史记录接口
│   │
│   ├── assets/                 # 静态资源
│   │   ├── fonts/
│   │   │   └── Azonix.otf      # 品牌字体
│   │   ├── images/
│   │   └── styles/
│   │       ├── theme.css       # 主题 CSS 变量（配色+字体+阴影）
│   │       ├── global.css      # 全局样式（重置+基础样式）
│   │       ├── effects.css     # 特效样式（发光+毛玻璃）
│   │       └── element-plus.css # Element Plus 主题覆盖
│   │
│   ├── components/             # 公共组件
│   │   ├── ParticlesBackground.vue  # 粒子连线背景
│   │   ├── ThemeToggle.vue     # 主题切换按钮
│   │   ├── Layout/             # 布局组件
│   │   │   ├── AppLayout.vue   # 主布局容器
│   │   │   ├── AppHeader.vue   # 顶部导航（毛玻璃）
│   │   │   ├── AppSidebar.vue  # 侧边栏（仅PC）
│   │   │   └── AppFooter.vue   # 底部
│   │   ├── ImageUpload.vue     # 图片上传组件
│   │   ├── VideoUpload.vue     # 视频上传组件
│   │   ├── DetectionResult.vue # 检测结果展示组件
│   │   ├── ChatBox.vue         # 聊天框组件
│   │   └── EmptyState.vue      # 空状态组件
│   │
│   ├── views/                  # 页面
│   │   ├── Login.vue           # 登录页
│   │   ├── Register.vue        # 注册页
│   │   ├── Detect.vue          # 检测页（核心）
│   │   ├── Knowledge.vue       # 知识库问答页
│   │   ├── History.vue         # 历史记录页
│   │   ├── HistoryDetail.vue   # 历史详情页
│   │   └── Profile.vue         # 个人中心
│   │
│   ├── router/                 # 路由
│   │   └── index.js
│   │
│   ├── store/                  # 状态管理（Pinia）
│   │   ├── index.js
│   │   └── modules/
│   │       ├── user.js         # 用户状态
│   │       └── theme.js        # 主题状态（亮色/暗色）
│   │
│   ├── utils/                  # 工具函数
│   │   ├── request.js          # 请求封装
│   │   ├── storage.js          # 本地存储
│   │   └── format.js           # 格式化工具
│   │
│   ├── App.vue                 # 根组件（含粒子背景）
│   └── main.js                 # 入口文件
│
├── index.html
├── package.json
├── vite.config.js
└── .env.example                # 环境变量示例
```

---

## 三、环境变量

```bash
# .env.development
VITE_API_BASE_URL=http://localhost:5000/api
VITE_APP_TITLE=智能检测系统

# .env.production
VITE_API_BASE_URL=/api
VITE_APP_TITLE=智能检测系统
```

---

## 四、API 封装

### 4.1 Axios 封装

```javascript
// api/axios.js
import axios from 'axios'
import { ElMessage } from 'element-plus'
import { useUserStore } from '@/store/modules/user'
import router from '@/router'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 30000
})

// 请求拦截器
request.interceptors.request.use(
  (config) => {
    const userStore = useUserStore()
    if (userStore.token) {
      config.headers.Authorization = `Bearer ${userStore.token}`
    }
    return config
  },
  (error) => Promise.reject(error)
)

// 响应拦截器
request.interceptors.response.use(
  (response) => {
    const res = response.data
    if (res.code === 0) {
      return res.data
    } else {
      ElMessage.error(res.message || '请求失败')
      return Promise.reject(new Error(res.message || '请求失败'))
    }
  },
  (error) => {
    if (error.response?.status === 401) {
      const userStore = useUserStore()
      userStore.logout()
      router.push('/login')
      ElMessage.error('登录已过期，请重新登录')
    } else {
      ElMessage.error(error.message || '网络错误')
    }
    return Promise.reject(error)
  }
)

export default request
```

### 4.2 认证接口

```javascript
// api/auth.js
import request from './axios'

export function register(data) {
  return request.post('/auth/register', data)
}

export function login(data) {
  return request.post('/auth/login', data)
}

export function getUserInfo() {
  return request.get('/auth/me')
}

export function changePassword(data) {
  return request.put('/auth/password', data)
}
```

### 4.3 检测接口

```javascript
// api/detection.js
import request from './axios'

export function detectImage(file, saveHistory = true) {
  const formData = new FormData()
  formData.append('file', file)
  formData.append('save_history', saveHistory)
  return request.post('/detect/image', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  })
}

export function detectVideo(file) {
  const formData = new FormData()
  formData.append('file', file)
  return request.post('/detect/video', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  })
}
```

### 4.4 知识库问答接口

```javascript
// api/knowledge.js
import request from './axios'

export function askQuestion(data) {
  return request.post('/knowledge/ask', data)
}

export function getChatHistory(params) {
  return request.get('/knowledge/history', { params })
}

export function deleteSession(sessionId) {
  return request.delete(`/knowledge/session/${sessionId}`)
}
```

### 4.5 历史记录接口

```javascript
// api/history.js
import request from './axios'

export function getHistoryList(params) {
  return request.get('/history', { params })
}

export function getHistoryDetail(id) {
  return request.get(`/history/${id}`)
}

export function deleteHistory(id) {
  return request.delete(`/history/${id}`)
}

export function batchDeleteHistory(ids) {
  return request.delete('/history', { data: { ids } })
}
```

---

## 五、路由配置

```javascript
// router/index.js
import { createRouter, createWebHistory } from 'vue-router'
import { useUserStore } from '@/store/modules/user'

const routes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/Login.vue'),
    meta: { title: '登录', requiresAuth: false }
  },
  {
    path: '/register',
    name: 'Register',
    component: () => import('@/views/Register.vue'),
    meta: { title: '注册', requiresAuth: false }
  },
  {
    path: '/',
    component: () => import('@/components/Layout/AppLayout.vue'),
    redirect: '/detect',
    meta: { requiresAuth: true },
    children: [
      {
        path: 'detect',
        name: 'Detect',
        component: () => import('@/views/Detect.vue'),
        meta: { title: '智能检测', icon: 'Search' }
      },
      {
        path: 'knowledge',
        name: 'Knowledge',
        component: () => import('@/views/Knowledge.vue'),
        meta: { title: 'AI 问答', icon: 'ChatDotRound' }
      },
      {
        path: 'history',
        name: 'History',
        component: () => import('@/views/History.vue'),
        meta: { title: '历史记录', icon: 'Clock' }
      },
      {
        path: 'history/:id',
        name: 'HistoryDetail',
        component: () => import('@/views/HistoryDetail.vue'),
        meta: { title: '检测详情', hidden: true }
      },
      {
        path: 'profile',
        name: 'Profile',
        component: () => import('@/views/Profile.vue'),
        meta: { title: '个人中心', icon: 'User' }
      }
    ]
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  document.title = to.meta.title ? `${to.meta.title} - 智能检测系统` : '智能检测系统'

  if (to.meta.requiresAuth && !userStore.token) {
    next({ path: '/login', query: { redirect: to.fullPath } })
  } else if ((to.path === '/login' || to.path === '/register') && userStore.token) {
    next('/')
  } else {
    next()
  }
})

export default router
```

---

## 六、状态管理（Pinia）

### 6.1 用户状态（user store）

```javascript
// store/modules/user.js
import { defineStore } from 'pinia'
import { login as loginApi, getUserInfo } from '@/api/auth'
import { setToken, getToken, removeToken } from '@/utils/storage'

export const useUserStore = defineStore('user', {
  state: () => ({
    token: getToken() || '',
    userInfo: null
  }),

  actions: {
    async login(loginForm) {
      const res = await loginApi(loginForm)
      this.token = res.access_token
      setToken(res.access_token)
      this.userInfo = res.user
      return res
    },

    async fetchUserInfo() {
      if (!this.token) return
      try {
        const res = await getUserInfo()
        this.userInfo = res
        return res
      } catch (e) {
        this.logout()
        throw e
      }
    },

    logout() {
      this.token = ''
      this.userInfo = null
      removeToken()
    }
  }
})
```

### 6.2 主题状态（theme store）

```javascript
// store/modules/theme.js
import { defineStore } from 'pinia'
import { getTheme, setTheme } from '@/utils/storage'

export const useThemeStore = defineStore('theme', {
  state: () => ({
    theme: getTheme() || 'light'
  }),

  getters: {
    isDark: (state) => state.theme === 'dark'
  },

  actions: {
    toggleTheme() {
      this.theme = this.theme === 'light' ? 'dark' : 'light'
      this.applyTheme()
    },

    setTheme(theme) {
      this.theme = theme
      this.applyTheme()
    },

    applyTheme() {
      if (this.theme === 'dark') {
        document.documentElement.setAttribute('data-theme', 'dark')
      } else {
        document.documentElement.removeAttribute('data-theme')
      }
      setTheme(this.theme)
    },

    initTheme() {
      this.applyTheme()
    }
  }
})
```

### 6.3 本地存储扩展

```javascript
// utils/storage.js
const TOKEN_KEY = 'token'
const USER_KEY = 'user_info'
const THEME_KEY = 'theme'

// ... 原有 token 方法 ...

export function setTheme(theme) {
  localStorage.setItem(THEME_KEY, theme)
}

export function getTheme() {
  const saved = localStorage.getItem(THEME_KEY)
  if (saved === 'light' || saved === 'dark') return saved
  const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches
  return prefersDark ? 'dark' : 'light'
}
```

---

## 七、页面详细设计

### 7.1 登录页 (Login.vue)

**功能**：
- 用户名密码登录
- 跳转到注册页
- 记住密码（可选）
- 响应式适配

**布局**：
- 左侧/顶部：品牌 Logo 和介绍
- 右侧/底部：登录表单
- 手机端：居中卡片式布局

**表单字段**：
| 字段 | 类型 | 必填 | 验证 |
|------|------|------|------|
| 用户名 | input | 是 | 3-50字符 |
| 密码 | password | 是 | 6-50字符 |

---

### 7.2 注册页 (Register.vue)

**功能**：
- 新用户注册
- 跳转到登录页
- 密码确认

**表单字段**：
| 字段 | 类型 | 必填 | 验证 |
|------|------|------|------|
| 用户名 | input | 是 | 3-50字符 |
| 邮箱 | input | 否 | 邮箱格式 |
| 密码 | password | 是 | 6-50字符 |
| 确认密码 | password | 是 | 与密码一致 |

---

### 7.3 检测页 (Detect.vue) —— 核心页

**功能模块**：

#### 7.3.1 上传区
- 点击上传图片
- 拖拽上传
- 选择文件
- 上传视频（Tab 切换）
- 手机端：拍照按钮（调用摄像头）

#### 7.3.2 检测中状态
- 加载动画
- 进度提示
- 取消按钮（可选）

#### 7.3.3 结果展示区
- 结果图片（带标注框）
- 检测统计卡片
  - 总检测数
  - 类别分布
  - 处理耗时
- 检测结果列表
  - 类别标签
  - 置信度
  - 坐标信息

#### 7.3.4 快捷问答
- "这是什么病？" 快捷提问按钮
- 点击直接跳转到 AI 问答页，带上检测结果

**布局（响应式）**：

| 设备 | 布局 |
|------|------|
| 电脑端（>1024px） | 左右分栏：左侧上传+结果图，右侧统计+列表 |
| 平板（768-1024px） | 上下布局：上传区在上，结果在下 |
| 手机（<768px） | 单列：大拍照按钮 → 结果图 → 统计 → 列表 |

---

### 7.4 知识库问答页 (Knowledge.vue)

**功能模块**：

#### 7.4.1 对话区域
- 消息列表（气泡式）
- 用户消息（右侧）
- AI 回复（左侧）
- 引用来源展示
- 打字机效果（可选）
- 滚动到底部

#### 7.4.2 输入区域
- 多行输入框
- 发送按钮
- 清空对话按钮
- 引用检测结果按钮（将当前检测结果作为上下文）

#### 7.4.3 侧边栏（仅PC）
- 新建对话
- 历史对话列表
- 删除对话

**手机端**：
- 顶部标题 + 菜单按钮
- 全屏聊天界面
- 底部输入框

---

### 7.5 历史记录页 (History.vue)

**功能**：
- 检测历史列表
- 按类型筛选（全部/图片/视频）
- 搜索（文件名）
- 分页
- 点击跳转到详情
- 删除单条 / 批量删除
- 空状态

**列表项展示**：
- 缩略图
- 文件名
- 类型标签
- 检测数量
- 时间

---

### 7.6 历史详情页 (HistoryDetail.vue)

**功能**：
- 展示完整检测结果
- 结果图片（可放大）
- 检测详情列表
- 检测统计
- 处理信息（耗时、模型版本等）
- 重新检测按钮
- 删除按钮
- 跳转到 AI 问答（带上结果）

---

### 7.7 个人中心 (Profile.vue)

**功能**：
- 用户信息展示
- 修改密码
- 修改头像（可选）
- 退出登录
- 检测数据统计（总数、近7天等，可选）

---

## 八、公共组件

### 8.1 ImageUpload.vue 图片上传组件

**Props**：
| 名称 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| accept | string | 'image/*' | 接受的文件类型 |
| maxSize | number | 10 | 最大大小（MB） |
| showPreview | boolean | true | 是否显示预览 |

**Events**：
| 名称 | 参数 | 说明 |
|------|------|------|
| change | file | 文件改变 |
| upload | file | 点击上传 |

**功能**：
- 点击上传
- 拖拽上传
- 图片预览
- 大小验证
- 格式验证
- 加载状态

---

### 8.2 DetectionResult.vue 检测结果展示组件

**Props**：
| 名称 | 类型 | 说明 |
|------|------|------|
| result | object | 检测结果数据 |
| imageUrl | string | 结果图片URL |

**功能**：
- 结果图片展示
- 检测统计
- 结果列表
- 类别颜色标识

---

### 8.3 ChatBox.vue 聊天框组件

**Props**：
| 名称 | 类型 | 说明 |
|------|------|------|
| messages | array | 消息列表 |
| loading | boolean | 是否加载中 |

**Events**：
| 名称 | 参数 | 说明 |
|------|------|------|
| send | content | 发送消息 |
| clear | - | 清空对话 |

**功能**：
- 消息气泡
- 时间显示
- 打字机效果
- 自动滚动
- 发送消息
- 快捷短语

---

## 九、响应式设计

### 断点设置

```css
/* 手机 */
@media (max-width: 767px) { ... }

/* 平板 */
@media (min-width: 768px) and (max-width: 1023px) { ... }

/* 电脑 */
@media (min-width: 1024px) { ... }
```

### 布局适配策略

| 组件 | 手机 (<768px) | 平板 (768-1023px) | 电脑 (>=1024px) |
|------|--------------|-------------------|----------------|
| 导航 | 底部 Tab 栏 | 顶部导航 | 侧边栏 + 顶部 |
| 检测页 | 单列 | 上下布局 | 左右分栏 |
| 问答页 | 全屏聊天 | 全屏聊天 | 左侧对话列表 + 右侧聊天 |
| 历史列表 | 卡片列表 | 两列卡片 | 表格 |
| 按钮 | 大按钮，全宽 | 正常按钮 | 正常按钮 |
| 字体 | 基准 14px | 基准 14px | 基准 14px |

---

## 十、工具函数

```javascript
// utils/storage.js
const TOKEN_KEY = 'token'
const USER_KEY = 'user_info'

export function setToken(token) {
  localStorage.setItem(TOKEN_KEY, token)
}

export function getToken() {
  return localStorage.getItem(TOKEN_KEY)
}

export function removeToken() {
  localStorage.removeItem(TOKEN_KEY)
  localStorage.removeItem(USER_KEY)
}

// utils/format.js
export function formatTime(date) {
  if (!date) return ''
  const d = new Date(date)
  const y = d.getFullYear()
  const m = String(d.getMonth() + 1).padStart(2, '0')
  const day = String(d.getDate()).padStart(2, '0')
  const h = String(d.getHours()).padStart(2, '0')
  const min = String(d.getMinutes()).padStart(2, '0')
  return `${y}-${m}-${day} ${h}:${min}`
}

export function formatFileSize(bytes) {
  if (bytes < 1024) return bytes + ' B'
  if (bytes < 1024 * 1024) return (bytes / 1024).toFixed(2) + ' KB'
  return (bytes / (1024 * 1024)).toFixed(2) + ' MB'
}
```

---

## 十一、启动方式

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

---

## 十二、package.json 依赖

```json
{
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0",
    "pinia": "^2.1.0",
    "element-plus": "^2.4.0",
    "@element-plus/icons-vue": "^2.3.0",
    "axios": "^1.6.0",
    "echarts": "^5.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.0",
    "vite": "^5.0.0",
    "sass": "^1.69.0"
  }
}
```
