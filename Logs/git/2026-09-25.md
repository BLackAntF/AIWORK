# Git 操作日志 - 2026-09-25

## 操作时间
2026-09-25

## 操作人
管理 Task

## 仓库信息
- 仓库：git@github.com:BLackAntF/AIWORK.git
- 分支：master
- 提交范围：92e2c33..313eef6

---

## 提交记录

### 提交 1：feat(mobile): 新增 uni-app 移动端 App
**Commit**: a01c2e0
**分支**: master
**变更文件数**: 46
**代码行数**: +14236 / -0

**修改内容**：

**移动端 App（新增，uni-app + Vue 3 + Pinia）：**
- 项目框架：vite 配置、pages.json（16 页面路由 + 5 项 TabBar）、manifest.json、uni.scss 全局样式
- 请求层：request.js（uni.request 封装，Token 注入、401 跳登录）、upload.js（uni.uploadFile 封装）
- API 层：auth / detect / knowledge / history / admin 接口文件
- 状态管理：store（user 登录态、chat 会话）
- 页面：登录、注册、首页、拍照检测、AI问答、检测历史（列表/详情）、病害档案详情、知识库（列表/详情）、个人中心、修改密码、管理员只读（用户/待审核/操作日志/数据看板）
- 公共组件：ChatBubble、ConfidenceTag、DiseaseCard、LoadingOverlay、EmptyState
- 后端零改动，复用 Web 端 Flask API

**说明**：移除 degit 模板自带的嵌套 `.git`（uni-preset-vue），使 mobile 目录纳入主仓库版本管理。

---

### 提交 2：feat(web): 知识库用户端页面、质量优化与文件上传知识库
**Commit**: bcb423a
**分支**: master
**变更文件数**: 22
**代码行数**: +3105 / -177

**修改内容**：

**前端：**
- 新增知识库用户端页面（KnowledgeList.vue 列表、KnowledgeDetail.vue 详情）
- 新增富文本编辑器组件（RichEditor.vue，基于 Tiptap）
- 管理端知识库页面新增文件上传弹窗、待审核列表与审核操作
- 新增用户端知识上传入口，补充相关 API（admin.js、knowledge.js）
- 引入 Tiptap 相关依赖（package.json、package-lock.json）
- 更新路由与侧边栏菜单（router/index.js、AppLayout.vue、AppSidebar.vue）

**后端：**
- Knowledge 模型新增 status / uploader_id / file_path 字段及 uploader 关系
- 新增文件解析服务（file_parser.py，支持 TXT/MD/DOCX/PDF）
- 新增用户上传接口（knowledge_user.py，需审核）、管理员上传接口、待审核列表与审核接口（admin/knowledge.py）
- 新增 HTML 处理工具（html_utils.py）
- 修复相关推荐接口路由冲突（knowledge.py）
- 新增数据库迁移脚本（fix_db.py、migrate.py）

---

### 提交 3：docs: 更新 PRD v2.0 并新增移动端 App 需求/开发/提示词文档
**Commit**: 313eef6
**分支**: master
**变更文件数**: 9
**代码行数**: +2172 / -79

**修改内容：**
- 更新 PRD 至 v2.0：新增移动端规划、RAG 问答架构定义、性能指标修订、版本路线图
- 新增 APP-需求文档.md（8 大功能模块、15 个页面、交互规范、验收标准）
- 新增 APP-开发文档.md（项目初始化、请求封装、状态管理、核心页面代码、6 阶段开发顺序）
- 新增 APP-开发提示词.md（Phase 1-6 分阶段开发提示词，供新 Task 直接执行）
- 新增任务文档：文件上传知识库、知识库用户端页面（含后端）、知识库质量优化
- 更新 TODO.md：标记移动端 App v1 完成，刷新下一步方向

---

## 统计汇总

| 指标 | 数量 |
|------|------|
| 提交总数 | 3 |
| 变更文件数 | 77 |
| 新增代码行数 | +19513 |
| 删除代码行数 | -256 |
| 净增代码行数 | +19257 |

---

## 清理操作

- 删除根目录临时文件：`grill-me.zip`、`test_knowledge.txt`
- 删除文件上传路径 Bug 产生的孤儿目录：`Projects/yolo-detection-system/static/`、`backend/routes/static/`
- 删除无效脚本：`backend/migrate_fields.py`（内容不完整）
- 删除空缓存目录：`Projects/yolo-detection-system/.npm-cache/`

---

## 推送状态
✅ 推送成功
远程：origin/master
范围：92e2c33..4e90f97

---

## 备注
- 本次提交涵盖移动端 App v1 开发、Web 端知识库功能完善、项目文档更新三大部分
- mobile 目录原为独立 git 仓库（degit 模板），已移除嵌套 .git 纳入主仓库
- 移动端 App 尚未在 H5 模式下逐页验证，后续需执行 Phase 6 优化

---

## 追加提交（备份）

### docs: 新增移动端App验证报告与项目交接总览，更新TODO
**Commit**: b0f8129

**变更内容：**
- 新增 `docs/移动端App验证-0925.md`：H5 验证报告（7 处 API 导入名错误 + 响应结构不符）+ 可执行修复提示词
- 新增 `docs/项目交接总览-0925.md`：新 Task 交接文档（项目概况、规范、进度、全部待办、完整提示词）
- 更新 `docs/TODO.md`：标记 H5 验证完成，指向交接总览，刷新下一步方向

**背景**：移动端 H5 验证完成，页面可渲染但数据层 API 契约不匹配；已生成修复任务提示词供新 Task 执行。

---

## 推送状态
✅ 已推送 origin/master（含追加提交）

---

## 追加提交（P3 最高优先级：修复移动端 API 契约）

### fix(mobile): 修复页面层 API 契约不匹配导致数据页面空白
**Commit**: dfac727
**分支**: master
**变更文件数**: 13
**代码行数**: +148 / -238

**修改内容：**

**问题定位**
- 经「读后端路由源码 + 实机调用接口」双向核对，确认 `api/*.js` 导出名与后端返回结构均正确，
  问题全部在**页面层**调用与读取方式
- 未改动 `api/*.js` 导出名，未改动后端任何代码（遵守任务约束）

**修复明细（7 处导入名 + 响应结构）**
- `pages/chat/index.vue`：导入改为 `askQuestion / getSessionList / getChatHistory / deleteSession`；
  会话列表字段改 `session_id / title / last_time`；历史改 `list`；
  **提问参数由后端完全不识别的 `image_url` 改为 `detection_context` + `detected_class_id`**；
  移除后端不返回的 `ConfidenceTag` 置信度展示；新增 `applyDetectionContext / refreshSessions / startNewSession` 等
- `pages/knowledge/list.vue`：导入 `getCategories`；分类改用 `category-list` 返回的字符串数组；筛选参数改 `category`
- `pages/knowledge/detail.vue`：导入 `getRelatedKnowledge`；`related` 改用 `list`；`category_name` → `category`
- `pages/admin/users.vue` / `pending.vue`：导入 `getUsers` / `getPendingKnowledge`，改用 `list`；`author_name` → `uploader_name`
- `pages/admin/logs.vue`：导入 `getLogs`，改用 `logs`
- `pages/admin/dashboard.vue`：导入 `getDashboardStats`；第 4 个统计卡改用后端真实存在的 `total_chats`；
  **删除后端无数据源的柱状图/折线图**，替换为「今日概览」（`today_users / today_detections / today_chats`）

**同类契约 bug 一并修复（报告未列出）**
- `pages/history/index.vue`、`pages/index/index.vue`：`data.items` → `data.list`；
  `getRecordImage` 改用 `result_path / original_path`；`getDiseaseName` 改用 `detections[].class_name`
- `pages/history/detail.vue`：`original_image / result_image` → `original_path / result_path`；
  `total_count` → `detection_count`；`detections` 取自 `detection_result.detections`
- `pages/mine/password.vue`：`changePassword` 改为单对象参数 `{ old_password, new_password }`
- `api/detect.js`：上传字段名 `image` → `file`（后端读 `request.files['file']`）
- `utils/format.js`：`getFullUrl` 去掉多余的 `/api` 前缀（原写法产出 `/api/static/...` 导致 404）

**验证结果（H5 实机逐页）**
- 环境：`python app.py`（:5000）+ `npm run dev:h5`（:5173），登录 `admin / password123`
- 通过页面（12 个）：首页、病害检测、AI问答、检测历史、知识库、我的、用户管理、待审核、操作日志、数据看板、知识详情、检测详情
- 全部页面：控制台无报错、无 4xx/5xx、图片无 404；TabBar 可见且切换正常
- 编译：dev server `ready in 2217ms`，仅 Sass `legacy-js-api` 弃用警告（无害）

**遗留问题（超出「只改前端页面」范围，已在 TODO 记录）**
1. `POST /api/knowledge/ask` 返回误导性 401：`LLM_USE_MOCK=false` 时外部 LLM 不可达 →
   `llm_service` 抛 `RuntimeError` → 被 `login_required` 的 `except Exception` 吞掉 → 返回 `code=401`
   （问答页已优雅降级，需后端补 try/except 或改用 mock）
2. `pages/archive/detail.vue`：后端不存在病害档案接口，需先新增后端接口

---

## 推送状态
✅ 已推送 origin/master
范围：63be0a7..dfac727

---

## 追加提交（文档）

### docs: 记录移动端 API 契约修复结果与遗留问题
**Commit**: b7553bc

**变更内容：**
- 更新 `docs/TODO.md`：标记 H5 逐页验证完成，列出遗留问题
- 更新 `docs/项目交接总览-0925.md`：同步移动端修复结果与下一步方向

---

## 追加提交（分阶段迭代 Phase 1：后端健壮性修复）

### fix(backend): 修复知识问答误报401并统一错误响应契约
**Commit**: 0e1dd2c
**分支**: master
**变更文件数**: 5
**代码行数**: +84 / -31

**修改内容：**

**问题根因**
- `POST /api/knowledge/ask` 在 `LLM_USE_MOCK=false` 且外部 LLM 不可达时返回误导性 `code=401`：
  `llm_service` 抛 `RuntimeError` → 被 `login_required` 的 `except Exception` 吞掉 → 误判为「Token无效」

**修复明细**
- `middleware/auth_middleware.py`：`fn()` 移出 try 块，仅捕获 `JWTExtendedException` / `PyJWTError`；
  新增 `_load_user()` 辅助函数；路由内部异常不再被吞成 401
- `app.py`：新增 `@app.errorhandler(Exception)` 兜底 + `404` 处理；500 统一走 `server_error()`
- `services/llm_service.py`：真实 LLM 调用增加 `timeout` 与降级回退 mock，回退时标记 `degraded=True`
- `routes/knowledge.py`：`generate_answer` 包 `try/except (RuntimeError, ValueError)` → `code=502`；
  限流阈值移入 `config`（`KNOWLEDGE_ASK_RATE_LIMIT_PER_MINUTE`），超限由裸 dict+HTTP429 改为 `code=429`；
  补 `not_found` 导入缺失（`/related` 接口原会 NameError）；响应 data 新增 `degraded` 字段
- `config.py`：新增 `LLM_TIMEOUT_SECONDS` / `LLM_FALLBACK_TO_MOCK` / `KNOWLEDGE_ASK_RATE_LIMIT_PER_MINUTE`

**验证结果（实机接口测试，:5000）**
| 用例 | 期望 | 实际 |
|------|------|------|
| 有效 token + 提问（外网 LLM 不可达） | `code=0` | `code=0`，`degraded=true`，sources=3 ✅ |
| 畸形 token | `code=401` | `code=401`（原先返回 HTTP 422 非约定结构）✅ |
| 无 token | `code=401` | `code=401` ✅ |
| 路由内部异常 | `code=500`（非 401） | `code=500 服务器内部错误` ✅ |
| 连续提问超限 | `code=429` | 第 19 次起 `code=429` ✅ |
| `/related` 不存在 id | `code=404` | `code=404 知识不存在` ✅ |

**结论**：`/api/knowledge/ask` 不再出现误导性 401，错误响应契约全链路统一为 `{code, message, data}`。

---

## 推送状态
✅ 已推送 origin/master
范围：dfac727..0e1dd2c（含文档提交 b7553bc）

---

## 追加提交（分阶段迭代 Phase 2：病害档案接口 + 关键词搜索修复）

### feat: 新增病害档案接口并修复关键词搜索500
**Commit**: bf54003
**分支**: master
**变更文件数**: 9
**代码行数**: +312 / -16

**变更内容：**

**① 新增病害档案 HTTP 接口（解封移动端档案页）**
- 新建 `routes/disease.py`，蓝图前缀 `/api/disease-profiles`：
  - `GET /api/disease-profiles`：`keyword?`（模糊匹配）/`page?`/`page_size?`(1..100 默认 20)
    → data `{list, total, page, page_size}`，列表项 `DiseaseProfile.to_dict()`
  - `GET /api/disease-profiles/<int:class_id>`：命中返回 `to_dict()`（**含 `occurrence`**，
    未使用故意不含该字段的 `get_profile_for_llm`）；未命中 `code=404`
  - 两个路由均加 `@login_required`；分页解析独立为 `_parse_pagination()`
- `routes/__init__.py` + `app.py` 注册蓝图（前缀不冲突，注册顺序追加在末位）

**② 移动端接入**
- `mobile/src/api/knowledge.js`：新增 `getDiseaseProfile(classId)` / `getDiseaseProfileList(params)`
- `mobile/src/pages/archive/detail.vue`：`loadProfile()` 由误调的 `getKnowledgeDetail(id)`
  改为 `getDiseaseProfile(id)`（`id` 即 `class_id`，来源 `DiseaseCard.vue` 跳转参数）
- 同页修正数据绑定 bug：副标题 `profile.class_name`（后端无此字段，恒空白）→ `profile.disease_id`

**③ 修复既有隐性 bug：SQLite 不支持 `concat` 函数**
- 现象：4 个列表接口的关键词搜索全部返回 `code=500`
  （`sqlite3.OperationalError: no such function: concat`）
- 根因：原写法 `col.like(func.concat('%', keyword, '%'))` 生成的 `concat(...)`
  在 SQLite 中不存在（SQLite 用 `||` 拼接）
- 修复：统一改为 ORM 原生 `col.contains(keyword)`（生成参数化 LIKE，跨库安全），
  并清理两处失效的 `from sqlalchemy import func` 导入
- 涉及文件：`routes/disease.py`、`routes/history.py`、`routes/admin/knowledge.py`、`routes/admin/users.py`

**④ 文档**
- 纳入本轮《前端统一与功能迭代实施计划-0925.md》（Phase 1-7 全量细节）

**验证结果（实机接口测试，:5000）**
| 用例 | 期望 | 实际 |
|------|------|------|
| `GET /api/disease-profiles/3` | `code=0` 含 `disease_name` | `code=0`，`晚疫病`，occurrence 40 字 / causes 63 字 ✅ |
| `GET /api/disease-profiles?page_size=3` | `{list,total,page,page_size}` | `total=10`，list 为 class_id 0/1/2 ✅ |
| `GET /api/disease-profiles?keyword=晚疫` | 模糊命中 1 条 | `total=1`，`晚疫病` ✅ |
| `GET /api/disease-profiles/99` | `code=404` | `code=404 病害档案不存在` ✅ |
| 无 token 访问档案接口 | `code=401` | `code=401 Token无效或已过期` ✅ |
| `GET /api/history?keyword=test` | 不再 500 | `code=0` ✅ |
| `GET /api/admin/knowledge?keyword=番茄` | 不再 500 | `code=0 total=8` ✅ |
| `GET /api/admin/users?keyword=admin` | 不再 500 | `code=0 total=1` ✅ |

**验证结果（移动端 H5 实机，:5173）**
- `#/pages/archive/detail?id=3` 渲染完整：标题「晚疫病 / LateBlight」、
  折叠面板 5 项（诱因/典型症状/发生规律/预防措施/治疗方案，首项默认展开且有真实中文正文）、
  底部「推荐药剂」卡片含多个药剂标签；未出现「档案不存在」空状态
- 控制台无报错；`/api/disease-profiles/3` HTTP 200

---

## 推送状态
✅ 已推送 origin/master
范围：fa7b3ca..bf54003

---

## 追加提交（分阶段迭代 Phase 3：站内通知系统）

### feat: 新增站内通知系统（后端模型/接口/触发点 + Web 铃铛与通知中心）
**Commit**: 793f077
**分支**: master
**变更文件数**: 14
**代码行数**: +1022 / -2

**变更内容：**

**① 后端模型与服务**
- `models.py` 新增 `Notification` 模型：`user_id`(FK+index) / `type`(index) / `title` / `content` /
  `is_read`(default False + index) / `related_type` / `related_id` / `created_at`(index)，
  含 4 个类型常量（`detection_completed` / `knowledge_approved` / `knowledge_rejected` / `system`）与 `to_dict()`
- 新建 `services/notification_service.py`：
  `create_notification`（不提交事务，由调用方决定时机）/ `notify_detection_completed` /
  `notify_knowledge_review` / `list_for_user` / `unread_count` / `mark_all_read`，单函数均 ≤20 行

**② 后端接口（前缀 `/api/notifications`，全部 `@login_required`）**
- `GET /`：`page?` / `page_size?`(1..100 默认 20) / `is_read?`(true|false) / `type?`（非法类型 `code=400`）
  → data `{list, total, page, page_size, unread_count}`
- `GET /unread-count` → `{unread_count}`
- `PUT /<int:id>/read` / `DELETE /<int:id>`：按 `id + user_id == current_user.id` 双条件查询，
  未命中统一 `code=404`（越权保护）
- `PUT /read-all` → `{updated_count}`
- `routes/__init__.py` + `app.py` 注册 `notifications_bp`（追加在末位，前缀无冲突）

**③ 后端触发点**
- `routes/detection.py::detect_image`：保存历史后写入 `detection_completed` 通知（标题含文件名与识别目标数）
- `routes/admin/knowledge.py::approve_knowledge`：审核后按 `kb.uploader_id` 写入通过/拒绝通知；
  `uploader_id` 为空（管理员上传/历史数据）时跳过

**④ 建表方式**
- 无 Alembic，新表由重启 `python app.py` 时 `db.create_all()` 自动创建，无需迁移脚本

**⑤ Web 端（样式零改动，仅复用既有类 `glass-card` / `btn-glow` / `el-*`）**
- 新增 `api/notifications.js`（5 个接口封装）
- 新增 `store/modules/notification.js`：`unreadCount` + `startPolling`/`stopPolling`（60s 轮询，失败静默）
- 新增 `components/NotificationBell.vue`：`el-badge` 未读角标 + `el-popover` 最近 5 条预览，
  含「全部已读」与「查看全部通知」；点击通知按 `related_type` 跳转检测详情 / 知识详情
- `AppHeader.vue` + `AdminHeader.vue`：铃铛挂在「主题切换」按钮左侧，header 挂载启动轮询、卸载停止
- 新增 `views/Notifications.vue` 通知中心 + `router/index.js` 新增 `/notifications` 路由

**验证结果（实机接口测试，:5000）**
| 用例 | 期望 | 实际 |
|------|------|------|
| 检测前通知列表 | 空 | `total=0 unread=0` ✅ |
| `POST /api/detect/image` 后 | 新增 1 条，未读 +1 | `code=0`，通知 `detection_completed`，`unread=1` ✅ |
| `GET /unread-count` | 1 | `unread_count=1` ✅ |
| testuser 上传知识 → admin 审核通过 | testuser 收到通知 | `knowledge_approved`「知识投稿已通过审核」✅ |
| 同上审核拒绝 | testuser 收到通知 | `knowledge_rejected`「知识投稿未通过审核」✅ |
| 跨用户 `PUT /read`（testuser 操作 admin 的通知） | `code=404` | `code=404 通知不存在` ✅ |
| 跨用户 `DELETE` | `code=404` | `code=404 通知不存在` ✅ |
| `PUT /<id>/read` 本人通知 | `code=0` 且 `is_read=true` | ✅ |
| `PUT /read-all` | 未读归零 | `updated_count=2`，未读 `0` ✅ |
| `DELETE /<id>` | 条数减少 | `total 3 → 2` ✅ |
| `is_read` / `type` 筛选 | 分别命中 | unread `1` / read `0` / type `1` ✅ |
| `?type=bad_type` | `code=400` | `code=400 无效的通知类型` ✅ |
| 无 token | `code=401` | `code=401 Token无效或已过期` ✅ |

**验证结果（Web 端实机，:5173，`admin / password123`）**
- 铃铛图标正常显示，红色未读角标为 `1`；popover 显示「通知」标题、「全部已读」、
  「图片检测完成」通知（含文件名与目标数）、底部「查看全部通知」
- `http://localhost:5173/notifications` 完整渲染：标题「通知中心」、副标题（总数/未读）、
  全部/未读/已读 筛选栏、通知卡片列表、分页控件
- 交互全部通过：标记已读（未读数减少、橙色左边线与红点消失）、全部已读（未读归零、无未读时按钮禁用）、
  未读筛选空状态、删除后条数减少
- 控制台无 JS 报错与 Vue 警告；`/api/notifications*` 请求均 HTTP 200 且 `code=0`；无样式错位

**排障记录**：首次浏览器验证失败（铃铛无角标、面板空白、路由不渲染），
原因为浏览器残留上一会话（移动端 H5）写入的旧 token 导致 `code=401`，
且 web 端为 `createWebHistory` 模式不能用 `#` 导航；清理 localStorage 重新登录后全部通过，
**非代码缺陷**。

---

## 推送状态
✅ 已推送 origin/master
范围：bf54003..793f077

---

## 追加提交（分阶段迭代 Phase 4：检测优化后端）

### feat: 检测优化后端（批量检测 + 历史高级筛选 + CSV 导出）
**Commit**: 9d0ec43
**分支**: master
**变更文件数**: 5
**代码行数**: +457 / -86

**变更内容：**

**① 配置与模型**
- `config.py` 新增 `MAX_BATCH_IMAGES`(20) / `EXPORT_MAX_ROWS`(5000)，均支持环境变量覆盖
- `models.py`：`DetectionHistory` 新增 `class_names`(String500, index) 与 `max_confidence`(Float)；
  `set_detection_result()` 同步写入冗余筛选字段（`class_summary` 键拼接、`detections` 最大置信度）；
  `to_dict()` 追加这两个字段（additive，列表响应外层结构不变）

**② 数据迁移（无 Alembic，沿用 fix_db.py 先例）**
- 新增 `migrate_history_fields.py`：`PRAGMA table_info` 检测后 `ALTER TABLE` 加列，
  再按 `detection_result` 反序列化回填旧行；已执行一次，回填 8 条

**③ 批量检测（routes/detection.py 重构）**
- 抽出 `_validate_image_file` / `_should_save_history` / `_save_history` / `_detect_one` 供单图与批量共用
- 新增 `POST /api/detect/batch`：multipart `files`（1..`MAX_BATCH_IMAGES`，超限 `code=400`）+ `save_history`
  → data `{list, total, success_count, failed_count}`；单图格式错误或异常降级为结果项（`success=false` + `error`），
  **不整体报错**
- `POST /api/detect/image` 行为与既有响应字段保持不变（仅 additive 多了 `original_filename`/`success`/`error`）
- 历史落库后统一写入 `detection_completed` 站内通知

**④ 历史高级筛选与导出（routes/history.py）**
- `get_history_list` 新增筛选：`start_date` / `end_date`（YYYY-MM-DD，结束日含当天）、
  `class_name`（模糊匹配 `class_names`）、`min_confidence`（0~1）、`model_version`（精确匹配）；
  解析失败返回 `code=400`；**响应外层结构不变**
- 新增 `GET /api/history/export`：同一套筛选参数，`utf-8-sig` CSV（Excel 打开中文不乱码），
  行数上限 `EXPORT_MAX_ROWS`；解析函数 `_parse_date_range` / `_parse_confidence` / `_parse_filters` / `_build_history_query` 抽离复用

**验证结果（实机接口测试，:5000）**
| 用例 | 期望 | 实际 |
|------|------|------|
| 迁移后 `GET /api/history` | 旧行有 `class_names`/`max_confidence` | id=7 `健康,白粉病,灰霉病,斑点病,炭疽病` / `0.9194` ✅ |
| `POST /api/detect/batch` 2 图 + 1 个 .txt | 部分失败不整体报错 | `code=0 total=3 success=2 failed=1`，失败项 `error=不支持的图片格式` ✅ |
| 批量成功项写入历史与通知 | 各生成 1 条历史 | id=9 / id=10，admin 未读 +2 ✅ |
| 批量无文件 | `code=400` | `code=400 请上传图片文件` ✅ |
| 批量 21 张 | `code=400` | `code=400 单次最多上传 20 张图片` ✅ |
| 批量 20 张 | `code=0` 全部成功 | `success_count=20` ✅ |
| `save_history=false` | 不落库 | 历史总数保持 9 ✅ |
| `start_date`/`end_date=今天` | 仅当日记录 | `total=3` ✅ |
| 2020 年日期范围 | 无记录 | `total=0` ✅ |
| `class_name=健康` + `min_confidence=0.8` | 命中 | `total=3` ✅ |
| `min_confidence=0.99` | 无命中 | `total=0` ✅ |
| `model_version=mock-yolo26s` | 命中全部 | `total=9` ✅ |
| `start_date=2026/09/25` | `code=400` | `code=400 start_date 格式应为 YYYY-MM-DD` ✅ |
| `min_confidence=2` | `code=400` | `code=400 min_confidence 必须为 0~1 的数字` ✅ |
| `GET /api/history/export?min_confidence=0.7` | 下载 CSV | HTTP 200、`Content-Type: text/csv; charset=utf-8`、BOM `EF BB BF` ✅ |
| 导出内容中文 | 不乱码 | 表头/病害名中文正常，`id=8`（低于阈值）被正确排除 ✅ |
| 空结果导出 | 仅表头 | 仅返回表头行 ✅ |
| 无 token 导出 | `code=401` | `code=401 Token无效或已过期` ✅ |
| `POST /api/detect/image` 回归 | 行为不变 | `code=0`，`detections[0].disease_profile` 仍存在，坏格式 `code=400` ✅ |

---

## 推送状态
✅ 已推送 origin/master
范围：793f077..9d0ec43

---

## 追加提交（分阶段迭代 Phase 5：检测优化 Web UI + 契约缺陷修复）

### feat(web): 检测优化前端UI（批量检测/历史筛选/CSV导出）并修复接口契约
**Commit**: 19c656b
**分支**: master
**变更文件数**: 11
**代码行数**: +636 / -159

**变更内容：**

**① 先修既有契约缺陷（否则新筛选挂在恒空列表上）**
- `views/History.vue`：列表取值 `res.data?.list || res.data?.items || res.data || []`（拦截器已剥离一层，恒为 `[]`）
  → 改为 `res.list`；模板字段 `thumbnail/image_url/file_name` → `result_path/original_path/original_filename`
- `views/HistoryDetail.vue`：`result_image/inference_time/file_name` → `result_path/processing_time/original_filename`；
  `detections` 改取 `detail.detection_result.detections`
- `views/Knowledge.vue`：`sources` 后端为**字符串数组**但模板读 `source.title` → 新增 `formatSource()` 兼容字符串/对象；
  会话时间 `session.updated_at` → `last_time`
- `api/axios.js`：新增 `responseType === 'blob'` 分支直接透传响应（否则 CSV 被当业务错误拦掉），
  且当 blob 实为 JSON 错误体时解析并弹出 message

**② 新增功能 UI（复用既有样式类，视觉零改动）**
- `api/detection.js` 加 `detectBatch`（超时 120s）；`api/history.js` 加 `exportHistory`（响应 blob）
- `views/Detect.vue`：上传区改 `multiple` 多选 + 缩略图预览网格（可删除/追加）+「开始批量检测 (N)」；
  新增批量结果 `el-table`（每图一行：结果图/文件名/检出病害/检出数/耗时/状态）+ 顶部三张 `.stat-card` 汇总；
  批量「AI 问诊」合并病害分布上下文跳转问答页
- `views/History.vue`：筛选栏新增 `el-date-picker`(daterange) / `el-select`(检出病害) / `el-select`(模型版本) /
  `el-input-number`(最低置信度 0~1) + 「导出 CSV」按钮（读取 `Content-Disposition` 取文件名）
- `views/Knowledge.vue`：仅做 `sources`/`last_time` 修正（多轮会话已生效）

**③ 后端配套修正**
- `app.py`：CORS 增加 `expose_headers: ['Content-Disposition']`，使前端可读取导出文件名
- `services/yolo_service.py`：**修复 Mock 类别与真实数据集/病害档案不一致缺陷** ——
  原 `MOCK_CLASSES` 仅 6 个简化中文名（健康/斑点病/白粉病/锈病/灰霉病/炭疽病），
  与 `dataset/Tomato/tomato/data.yaml` 的 10 类顺序及 `disease_profiles.class_id` 完全对不上，
  导致 `class_id=3` 检出「锈病」却关联「晚疫病」档案；改为与 data.yaml 一致的 10 类中文名
- `mobile/src/pages/detect/index.vue`、`mobile/src/pages/history/detail.vue`：同步健康分支判断 `'Healthy'` → `'健康叶片'`
  （原值在任何版本下都从未命中，健康提示从未显示）

**验证结果（接口层实机，:5000）**
| 用例 | 期望 | 实际 |
|------|------|------|
| `POST /api/detect/batch` 3 张真实图 | 全部成功 | `code=0 total=3 success=3 failed=0`，含 result_image/class_summary ✅ |
| 批量 21 张 | `code=400` | `code=400 单次最多上传 20 张图片` ✅ |
| 批量空 files | `code=400` | `code=400 请上传图片文件` ✅ |
| 类别与档案一致性 | `class_id` 对应同名档案 | `class_id=7` → `蜘蛛螨危害` → 档案 `蜘蛛螨危害` ✅（修复前为 `锈病`/`晚疫病` 错配） |
| `GET /api/history` | 列表有数据且含新字段 | `total` 有值，项含 `class_names`/`max_confidence` ✅ |
| `class_name=早疫病` 筛选 | 命中 | `total=2` ✅ |
| `min_confidence=0.9` 筛选 | 命中 | `total=7` ✅ |
| `start_date=2026/01/01` | `code=400` | `code=400 start_date 格式应为 YYYY-MM-DD` ✅ |
| `min_confidence=2` | `code=400` | `code=400 min_confidence 必须为 0~1 的数字` ✅ |
| `GET /api/history/export` | CSV 下载 | HTTP 200、`text/csv; charset=utf-8`、`Content-Disposition: attachment; filename="history_20260925_161525.csv"` ✅ |
| `GET /api/disease-profiles/3` | 档案可用 | `code=0`，`晚疫病` ✅ |

**验证结果（Web 端实机，:5173，`admin / password123`）**
- 历史记录页：列表 16 条有数据，缩略图/文件名/病害标签/最高置信度/检测数量/时间均正常（修复前恒空）
- 高级筛选：日期范围、检出病害、最低置信度、模型版本均生效且无 console 报错
- 导出 CSV：请求 200、响应为 CSV 并触发下载
- 批量检测：canvas 合成 3 张图注入隐藏 file input → 出现缩略图网格、按钮变「开始批量检测」→
  右侧出现批量结果表格（结果图/文件名/检出病害/检出数/耗时/状态）+ 三张统计卡有合理数值，
  `POST /api/detect/batch` 200，无 console 报错
- AI 问答页：引用来源正确显示文本（如「栽培技术要点。测试知识内容…」）；
  会话时间「11小时前」「8小时前」渲染正常（此前疑似的异常实为**会话标题本身为 "t"**，非 bug）
- 全流程：无业务 console 错误、无 4xx/5xx、无图片 404

**排障记录**：
1. 批量检测首次验证 BLOCKED —— 自动化无法点击隐藏 `<input type=file>`，改用 `browser_evaluate` 跨域 fetch 图片又被拦截
   （后端 CORS 仅对 `/api/*` 生效，`/static/*` 无 CORS 头），最终改为 **canvas 在页内合成 JPEG 再注入**，验证通过，非代码缺陷
2. 端口冲突：web 与移动端 H5 默认均 5173，本次先释放残留 H5 dev server 再启动 web，确保落在 5173（CORS 白名单端口）

**说明**：本轮严格遵守「web 视觉零改动」，未触碰 `assets/styles/*` 与 `ParticlesBackground.vue`，新增控件全部复用既有样式类。

---

## 推送状态
✅ 已推送 origin/master
范围：acfe163..19c656b

---

## 追加提交（分阶段迭代 Phase 6：移动端视觉对齐 Web）

### feat(mobile): Phase 6 移动端视觉对齐Web（tokens/PageHeader/AppIcon去emoji/语义色）
**Commit**: da30fb2
**分支**: master
**变更文件数**: 27
**代码行数**: +630 / -703

**修改内容：**

**① 设计 Token 统一（对齐 web `theme.css` 浅色值）**
- 新建 `mobile/src/styles/tokens.scss`：`--brand #E07A5F` / `--brand-light #F29780` / `--brand-dark #C96247`、
  `--text-1/2/3/disabled`、`--bg-1/2/3`、`--border/-light`、语义色 `--success #52C41A` / `--warning #FAAD14` /
  `--error #FF4D4F` / `--info #1890FF`、半径与阴影；`App.vue` 全局 `@use`（替代 `@import` 消除 deprecation 警告）
- `uni.scss` 的 `$primary-color` 等改为与 tokens 同值，注释标明「数值唯一来源见 styles/tokens.scss」
- 组件 token 化：`LoadingOverlay`（bg/border/radius）、`ChatBubble`（气泡/自聊渐变/圆角/阴影）、
  `ConfidenceTag`（低可信灰 600 与背景 12%）

**② 统一渐变头部组件 PageHeader**
- 新建 `components/PageHeader.vue`（props title/subtitle/showBack，slot actions；返回用 getCurrentPages/
  navigateBack/switchTab），替换 10+ 页面各自复制的 `.page-header` 块并删除本地重复样式：
  login/register/index/detect/chat/history(index/detail)/archive/detail/knowledge(list/detail)/
  mine(index/password)/admin(dashboard/logs/users/pending)
- 头部统一 `linear-gradient(135deg, var(--brand), var(--brand-dark))`

**③ emoji → AppIcon（base64 SVG，兼容 App 端）**
- 新建 `utils/icons.js`（ICON_SHAPES 30 个图标 + iconSrc(name,color) 返回带缓存的 base64 data-URI）
- 新建 `components/AppIcon.vue`（`<image mode="aspectFit" :src>` 渲染）
- 全量替换 emoji：`EmptyState`（icon prop → name）、archive/detail、history/index、detect、chat、mine、admin/*、
  knowledge/list 等；`grep emoji` 命中归零

**④ 语义色映射收尾（web 端语义色：warning #FAAD14 / error #FF4D4F / info #1890FF）**
- `detect/index.vue`：`.item-tag` #F44336→var(--error)（背景 rgba 同步 255,77,79,0.1）；
  `.confidence-fill` 渐变 → `var(--error) → var(--warning) → var(--brand)`；
  `.ai-btn` #2196F3→var(--info)
- `history/detail.vue`：同一套 `.item-tag` / `.confidence-fill` 渐变替换
- `archive/detail.vue` + `DiseaseCard.vue`：农药标签 `.pesticide-item` / `.pesticide-tag`
  #FF9800→var(--warning)，背景 rgba(255,152,0,0.15)→rgba(250,173,20,0.15)
- `mine/index.vue`：退出登录按钮 color/border #F44336→var(--error)，`uni.showModal` confirmColor 改 #FF4D4F（JS 上下文）
- `admin/*.vue`：read-only-hint/item-status/role-tag 改 var(--warning) + rgba(250,173,20,0.1)；
  pending 的 border-left 6rpx solid var(--warning)
- `grep` 全库确认 #FF9800/#F44336/#2196F3/#9E9E9E 残留为零（剩余匹配仅为 token 定义与 JS 上下文色值）

**验证结果（H5 实机，:5174，admin/password123）**
- 环境：dev:h5 端口 5173 被占自动切 5174；`ready in 2782ms`，编译无错误
- 首页：header-bg 品牌渐变生效、AppIcon 全部渲染为 base64 SVG data-URI（无 emoji）
- 历史详情（id=35）：`.item-tag` 早疫病 rgb(255,77,79)+rgba(255,77,79,0.1)、健康叶片 rgb(224,122,95)；
  `.confidence-fill` 渐变 `rgb(255,77,79) → rgb(250,173,20) → rgb(224,122,95)` 与 web 对齐
- 我的：退出登录按钮 color/border 均 rgb(255,77,79) = var(--error)
- 问答：chat-header 渐变 `rgb(224,122,95) → rgb(201,98,71)` = brand → brand-dark
- admin 数据看板：read-only-hint 背景 rgba(250,173,20,0.1)；role-tag.admin/item-status 源码 var(--warning)
- 知识库列表/详情：正常渲染，知识详情跳转正常
- 无业务 console 错误、无 4xx/5xx

**说明**：严格遵守「web 视觉零改动」，未触碰 `frontend/src/assets/styles/*` 与 web 组件；
DiseaseCard 农药标签源码已替换并经 grep 零残留（该卡片需真实检测含防治方案数据才渲染，H5 mock 数据未触发）。

---

## 推送状态
✅ 已推送 origin/master
范围：cb0b53c..da30fb2

---

## 追加提交（分阶段迭代 Phase 7：移动端交互与问答增强 + 全链路回归）

### feat(mobile): Phase 7 移动端交互增强（Token生命周期/去硬编码/统一错误/下拉刷新/通知中心/问答打字机）
**Commit**: d29f67c
**分支**: master
**变更文件数**: 22
**代码行数**: +582 / -50

**修改内容：**

**① Token 生命周期（store/user.js + utils/error.js + 请求层）**
- `store/user.js` 新增 `clearAuth()`：清 token/userInfo + `uni.removeStorageSync`，**不跳转**（供 token 过期静默清理用）；`logout()` 改由 clearAuth + reLaunch 登录页
- 新建 `utils/error.js`：`showError(e, fallback)` 统一错误提示（toast `e.message || fallback` + console.error）；
  `handleTokenExpired()` 统一 401 处理——`removeStorageSync`、已登录页跳过、**单飞保护**（`isTokenRedirecting` 防连跳）、
  `useUserStore().clearAuth()`、toast「登录已过期…」、1s 后 reLaunch 登录页
- `api/request.js` / `api/upload.js`：401 分支改调统一 `handleTokenExpired`

**② 去硬编码（127.0.0.1 零残留）**
- 三处 `'http://127.0.0.1:5000/api'` 回退全部删除：`request.js` / `upload.js`（`#ifndef H5` 分支读
  `import.meta.env.VITE_API_BASE_URL`）、`utils/format.js`（`(env || '').replace('/api','')`）
- 新建 `.env.production` 模板：`VITE_API_BASE_URL=http://127.0.0.1:5000/api`（App 端打包用）

**③ 统一错误处理（showError 落地 13 处 catch）**
- admin 4 页（dashboard/logs/users/pending）、history/index、knowledge/list、mine/password、
  archive/detail、history/detail、knowledge/detail、chat/index（switchSession/deleteSession）全部
  `uni.showToast({title:'…'})` → `showError(e, '加载失败'/'修改失败…')`
- `grep` 验证 pages 目录下旧式错误 toast 零残留

**④ 下拉刷新（onPullDownRefresh + pages.json enablePullDownRefresh）**
- pages.json 为 index/index、mine/index、archive/detail、history/detail、knowledge/detail 开启
  `enablePullDownRefresh`；3 个详情页 add `onPullDownRefresh` + finally `uni.stopPullDownRefresh()`

**⑤ 通知中心（消费 Phase 3 接口）**
- 新建 `api/notifications.js`：`getNotifications / getUnreadCount / markNotificationRead / markAllNotificationsRead / deleteNotification`
- 新建 `pages/notifications/index.vue`：PageHeader（actions「全部已读」）、scroll-view refresher、
  EmptyState、类型中文映射（检测完成/审核通过/审核未过/系统）、未读左边线+红点、
  标已读/删除（阻止冒泡）、分页（loadList + onLoadMore）、下拉刷新
- `mine/index.vue`：新增「站内通知」menu 入口 + 未读角标 `.badge`（>99 显示 99+），
  `onShow` 拉取 `getUnreadCount`、`onPullDownRefresh` 同步刷新
- **回归发现并修复 1 个 bug**：通知页首次进入永不发列表请求 —— `loadList` 仅挂在
  refresher/scrolltolower 上，缺初始化调用；补 `onLoad(() => loadList(true))`（H5 实机从
  「暂无通知」修复为 10 条列表可见）

**⑥ 问答增强（chat/index.vue）**
- `toMessage` 保留 `metadata.sources`，assistant 气泡下渲染「参考来源」块（`source.title || file_name`，
  click 跳知识详情）
- 新增 `typeWriter()` 逐字输出（25ms/2 字符，完成后 resolve）；sendMessage：thinking 气泡 →
  收到回复 push `content:''` 空气泡（替换思考态）→ 打字机填充 → 新会话刷新列表
- `session_id` 续接确认：askQuestion 返回 `session_id`，历史消息逐条携带且多轮连贯

**验证结果（H5 实机，:5174，admin/password123）**
- 首页：admin 已登录、最近检测列表、5 Tab 正常
- 问答：发送「早疫病初期怎么识别？」→ thinking 出现→消失→回复打字机完整渲染；
  `.sources-block` 由 2 增至 3（历史 2 + 新回复 1，共 9 条来源项）
- 通知中心：列表 10 条全部未读 →「标为已读」（10→9）→「全部已读」（归零）→「删除」（10→9）全部通过；
  我的页角标「27」与后端 unread-count 一致
- 历史列表/详情、知识库列表/详情（id=21）、档案详情（id=1 早疫病完整渲染+推荐药剂）、
  修改密码、admin 4 页（看板/用户/待审核/日志）全部正常渲染
- 无业务 console 错误；`/api/notifications*` 均 HTTP 200
- **排障记录**：直接改 hash 二次导航到同一路径时 uni-app H5 复用页面实例、
  `onLoad`/query 不更新导致误判「档案不存在/知识不存在」——真实用户 uni.navigateTo 传参正常，
  非代码缺陷（已用 `uni.redirectTo` 内跳验证）

**说明**：严格遵守「web 视觉零改动」，全部改动仅限 `mobile/` 目录。

---

## 推送状态
✅ 已推送 origin/master
范围：6f321a4..d29f67c

---

## 追加提交（Phase 7 日志）

### docs: 追加2026-09-25 Git日志（Phase 7 移动端交互增强 + 全链路回归）
**Commit**: d44585b

---

## 推送状态
✅ 已推送 origin/master
范围：d29f67c..d44585b（项目代码 d29f67c + 日志 d44585b）

---

## 追加验证记录（Phase 7 下拉刷新运行时真实性验证）

### 结论
下拉刷新链路在 H5 运行时**真实工作**（编译期 flag + 组件渲染 + 触发请求 + 状态机闭环全部验证通过）。

### 排障记录：`__UNI_FEATURE_PULL_DOWN_REFRESH__` 运行时为 false 的根因
- **现象**：浏览器运行时读 `__UNI_FEATURE_PULL_DOWN_REFRESH__` 输出 `false`（`__UNI_FEATURE_NAVIGATIONBAR__` 为 `true`），
  `<uni-page-refresh>` 元素不存在，`uni.startPullDownRefresh()` 调用成功但无任何效果
- **排查链**：
  1. `pages.json` 确认 6 页 `enablePullDownRefresh: true`（index/mine/archive-detail/history-detail/knowledge-detail/notifications），配置无误
  2. `uni-cli-shared/dist/vite/features.js::initPagesFeature` 确认任一页 `enablePullDownRefresh` 即置 `pullDownRefresh=true`，计算逻辑无误
  3. **根因**：端口 5173 同时存在两个 dev server 进程 —— 旧进程 17:12 启动（早于 pages.json 修改 19:48），仅监听 `::1`（localhost）；
     新进程 20:21 启动（`--port 5173 --host`，pages.json 修改之后），监听 `::`。
     浏览器访问 `localhost:5173` 命中旧进程 → 拿到**编译期旧快照**的 flag=false
- **修复**：停掉旧 dev server 进程，仅保留新进程后，无需改任何代码

### 运行时验证证据（H5 实机，:5173）
| 验证项 | 期望 | 实际 |
|------|------|------|
| 编译期 flag | `true` | `__UNI_FEATURE_PULL_DOWN_REFRESH__=true` ✅（修复前 false） |
| 刷新组件渲染 | `<uni-page-refresh>` 存在 | `refreshEl=true`，含刷新指示器 SVG（margin-top:44px）✅ |
| `uni.startPullDownRefresh()` 调用 | 成功 | `called=true`，无异常 ✅ |
| 触发网络刷新 | 产生新 `/api/history` 请求 | `performance` 计数 3→4（`/api/history?page=1&page_size=5`）✅ |
| 刷新完成回弹 | 状态机进入 restoring | `uni-page-refresh--restoring` class 出现 ✅ |

**说明**：Phase 7 全链路回归至此收尾（登录/检测/历史/问答/通知/移动端各页此前各阶段已逐项验证）；本轮仅补下拉刷新运行时验证，**无任何代码改动**，唯一操作是清理残留的旧 dev server 进程。

---

## 追加提交（Phase 7 验证日志）

### docs: 追加2026-09-25 Git日志（Phase 7 下拉刷新运行时验证）
**Commit**: 9d7b617
**分支**: master

---

## 追加提交（功能迭代第 1 项：AI 问答增强）

### feat(backend): 重写LLM身份规范并接通真实大模型链路
**Commit**: 6f34b60
**分支**: master

**修改内容：**

**① system prompt 重写（services/llm_service.py）**
- 角色设定「番茄卫士」资深番茄种植专家，明确职责边界
- 上下文优先级：检测结果 → 病害档案 → 相关知识 → 通用知识
- 必需结构化输出模板：诊断结论 → 置信度评估（检测置信度回显 + 判断置信度评级）→ 症状依据 → 防治方案（农业/化学）→ 药剂推荐 → 注意事项
- 行为红线：信息不足时明确说明、禁止「根治/100%有效」保证、不确定时建议咨询植保站

**② 真实 LLM 链路接通（配置排障）**
- 原 `LLM_BASE_URL=https://chai.hay.hk/v1` DNS 解析失败（`httpcore.ConnectError: getaddrinfo failed`）→ 降级 mock 返回 `degraded=true`
- 更换可用网关 `https://8888ok.cc/v1` + key，`LLM_MODEL=deepseek-v4-flash`，`LLM_USE_MOCK=false`
- 修改 `.env` 后重启后端（`load_dotenv()` 仅进程启动时生效）

**验证结果（实机接口测试，:5000）**
| 用例 | 期望 | 实际 |
|------|------|------|
| 带 `detected_class_id=1` 提问 | `code=0` 非降级 | `degraded=false`，`disease_profile` 存在，输出遵循模板（检测置信度 87.5% + 判断置信度高）✅ |
| `/api/models` 网关可用性 | 列出模型 | `deepseek-v4-flash` / `deepseek-v4.1-flash` ✅ |
| 无上下文提问 | 仍可回答 | `code=0`，无 profile ✅ |

---

## 推送状态
✅ 已推送 origin/master
范围：9d7b617..6f34b60

---

## 追加提交（功能迭代第 2 项：病害态势统计 - 后端）

### feat(backend): 病害态势统计接口（分布+趋势，过滤健康类并归一化旧类别别名）
**Commit**: e4ff46c
**分支**: master
**变更文件数**: 1
**代码行数**: +107 / -0

**修改内容：**

**① `routes/admin/stats.py` 新增 2 个接口（均 `@admin_required`）**
- `GET /api/admin/stats/disease-distribution?days=30`：按日范围统计各病害检出分布
  - 基于 `DetectionHistory.class_names`（逗号分隔的冗余筛选字段）
  - 过滤 `NON_DISEASE_CLASSES`（健康/健康叶片/健康叶子/正常叶片）
  - 旧数据类别别名归一化：`斑点病` → `细菌性斑点病`（旧数据集简名）
  - 返回 `{days, total_records, distribution:{name:count}}`（按检出数降序）
- `GET /api/admin/stats/disease-trend?days=7`：近 N 日各病害检出趋势
  - 按 `YYYY-MM-DD → 病害名 → 计数` 聚合，补齐缺失日期为 0，保证各 series 等长
  - 返回 `{days, dates, series:[{name,data}]}`

**② 数据口径对齐（查库校准）**
- 通过 `SELECT DISTINCT class_names` 确认库内存在两代类名：旧数据（健康/斑点病/灰霉病/白粉病/锈病/炭疽病）与新模型（10 类 data.yaml）
- `NON_DISEASE_CLASSES` 补入旧名「健康」；`CLASS_ALIASES` 将「斑点病」归一化到当前类名

**验证结果（实机接口测试，:5000，admin/password123）**
| 用例 | 期望 | 实际 |
|------|------|------|
| `GET /stats/disease-distribution?days=30` | `code=0` | `total_records=30`，无健康类，细菌性斑点病 16（9+7 合并）✅ |
| `GET /stats/disease-trend?days=7` | 各 series 等长补零 | `dates` 7 个、13 条 series 均长度 7 ✅ |
| 权限校验 | admin 专属 | 非 admin `code=403` ✅ |

---

## 推送状态
✅ 已推送 origin/master
范围：6f34b60..e4ff46c

---

## 追加提交（功能迭代第 2 项：病害态势统计 - 前端）

### feat(web): 管理看板病害态势可视化（分布环形图 + 趋势折线，四图联动）
**Commit**: 待提交
**分支**: master
**变更文件数**: 2
**代码行数**: +150 / -4（约）

**修改内容：**

**① `frontend/src/api/admin.js` 新增 2 个接口封装**
- `getDiseaseDistribution(days=30)` → `/admin/stats/disease-distribution`
- `getDiseaseTrend(days=7)` → `/admin/stats/disease-trend`

**② `frontend/src/views/admin/Dashboard.vue` 新增 2 个图表卡片（复用既有样式类，视觉零改动）**
- 「病害检出分布」：环形饼图（radius 38%~68%），legend 右侧垂直 scroll，tooltip `{b}: {c} 次 ({d}%)`，按检出数降序
- 「病害检出趋势」：多系列折线，legend 底部 scroll，yAxis `minInterval:1`，各 series 用 15 色 `DISEASE_PALETTE` 循环取色
- 四个图表卡片的天数下拉（近7天/近30天）统一绑定 `trendDays` 并走 `handleTrendChange`：同步刷新用户/检测/病害分布/病害趋势
- `updateDiseaseCharts` 用 `setOption(..., true)`（notMerge）防止切换天数后旧 series 残留
- `fetchDiseaseStats` 用 `Promise.all` 并行拉取两个接口，失败时渲染空图不阻塞页面
- `onMounted` / `handleResize` / `onUnmounted` 同步接入两个新图表生命周期

**验证结果（浏览器实机，:5173，admin/password123）**
| 验证项 | 期望 | 实际 |
|------|------|------|
| 四个图表卡片渲染 | 卡片 + canvas | 4 张 canvas 均 784x450 ✅ |
| `disease-distribution?days=7` | `code=0` | 13 种病害、无健康类、细菌性斑点病 16（归一化合并）✅ |
| `disease-trend?days=7` | series 等长 | 7 个日期、13 条 series 各 7 个数据点 ✅ |
| 下拉联动 | 切换近30天四图同步刷新 | 四个 select 均变「近30天」，四个接口均发出 `days=30` 请求 ✅ |
| 控制台 | 无运行时错误 | 新 tab 加载 console 零错误（旧日志为已停移动端服务残留）✅ |

**说明**：本轮严格遵守「web 视觉零改动」，未触碰 `assets/styles/*` 与 `ParticlesBackground.vue`，新增图表全部复用既有 `chart-card`/`glass-card` 样式类。

---

## 推送状态
✅ 已推送 origin/master
范围：e4ff46c..（f2-2 前端提交）