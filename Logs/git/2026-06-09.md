# Git Log

## 2026-06-09

### feat: 初始化 Memory 目录结构

- 初始化 Git 仓库，关联远程 `git@github.com:BLackAntF/AIWORK.git`
- 创建 `.gitignore`，忽略 Tools/ 等目录
- 创建 `Memory/` 目录结构:
  - `MEMORY.md` - 记忆存储
  - `STANDARDS.md` - 规范文档
  - `CONSTRAINTS.md` - 约束文档
- 首次提交并推送到 GitHub master 分支

### feat: 添加 AI Coding 规范到 STANDARDS.md

- 代码质量：函数不超过 20 行、类型提示、禁止 Magic Value、防御性编程、第三方库白名单
- 安全红线：禁止硬编码密钥、参数化查询、输入验证、禁止 eval/SQL拼接、权限校验
- 工程流程：人工 review、单元测试、禁止直推主分支、分层约束体系

### refactor: 分离规范与约束到各自文件

- `STANDARDS.md`：保留推荐性规范（函数不超过20行、类型提示、防御性编程）
- `CONSTRAINTS.md`：移入禁止性约束（安全红线、工程流程、代码质量禁止项）

### fix: 根据代码审查报告修复6个问题

1. **parser.py** - 代码块索引可能越界：使用 `block_index = len(blocks)` 在添加前获取索引
2. **downloader.py** - 图片下载路径未验证：添加 `img_dir.mkdir(parents=True, exist_ok=True)`
3. **scraper.py** - 资源泄漏风险：将浏览器创建移到重试循环外部，复用单个浏览器实例
4. **cli.py** - 日志信息不准确：使用实际下载成功的图片数 `len(url_map)`
5. **utils.py** - 异常处理过于宽泛：明确捕获 `ValueError, KeyError, IndexError`
6. **utils.py** - 日志配置检查不健壮：使用标志位 `_logging_configured`

### update: 添加版本管理规则到 CONSTRAINTS.md

- **版本管理规则**: 前端、后端任务完成后必须进行 Git 管理（commit、push 并记录日志到 `D:\Develop\CODE\AIWORK\Logs\git\`）

### fix: 根据前端代码审查报告修复7个问题

1. **types.ts** - 类型定义不一致：统一 `userId` 和 `id` 为 `string` 类型
2. **items.ts** - API 参数设计问题：分离 `getItems()` 和 `getItem(id)` 两个函数
3. **Login.tsx** - 错误处理过于宽泛：根据 HTTP 状态码区分网络错误、401、400 等
4. **Register.tsx** - 错误处理过于宽泛：区分 409、400 等错误；添加密码强度验证（长度、大小写、数字）
5. **Dashboard.tsx** - 缺少请求取消机制：使用 `AbortController` 取消进行中的请求
6. **Dashboard.tsx** - 使用原生 confirm：替换为自定义确认对话框组件
7. **AuthContext.tsx** - localStorage 缺少错误处理：使用 try-catch 包裹 `JSON.parse`

### feat: 添加完整项目文件

- 更新 `.gitignore`，添加 `Projects/frontend/node_modules/` 和 `Projects/backend-template/node_modules/`
- 添加 `Projects/wechat-admin/backend-template/` - Next.js 后端模板
- 添加 `Projects/wechat-admin/frontend/` - React + TypeScript 前端项目
- 添加 `Reports/` - 代码审查报告
- 添加 `Skills/` - Agent 技能配置
- 共提交 67 个文件

### refactor: 重组项目结构

- 在 `Projects/` 下新建项目目录 `wechat-admin/`
- 将 `frontend/` 和 `backend-template/` 移动到 `Projects/wechat-admin/` 下
- 新项目路径：`D:\Develop\CODE\AIWORK\Projects\wechat-admin\frontend`
- 新项目路径：`D:\Develop\CODE\AIWORK\Projects\wechat-admin\backend-template`
